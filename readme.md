# Modern Fuzzy Match For VIM

- Using Vim's built-in matchfuzzypos function, a million level search takes less than 1 second

- All use floating windows

- Combined with coc.nvim, it can partially replace CocList

- Combining asynchronous functions with RG search

- High performance preview

- Delayed rendering, only displaying what is visible to the user
